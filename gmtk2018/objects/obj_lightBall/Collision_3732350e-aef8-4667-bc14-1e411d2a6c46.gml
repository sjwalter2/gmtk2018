/// @description Insert description here
// You can write your code in this editor
if(!charging)
{
	onPlatform = 1;
	if(other != lastPlatform && other != currentPlatform)
	{
		lastPlatform = currentPlatform	
		currentPlatform = other
	}
}