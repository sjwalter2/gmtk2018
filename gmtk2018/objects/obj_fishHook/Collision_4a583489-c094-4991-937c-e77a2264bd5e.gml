/// @description Insert description here
// You can write your code in this editor
if(other.id != creator.id && alarm_get(1) = -1 && !other.tempInvincibility)
{
	attached = other;
	grav = 0
	hitTarget = 1;
}